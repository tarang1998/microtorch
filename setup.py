from setuptools import setup, find_packages

setup(
    name="microtorch",
    version="0.1.0",
    author="Tarang Nair",
    description="Artificial Intelligence library",
    packages=find_packages(),
    install_requires=[],  # Add any required packages
    python_requires=">=3.7",
)
